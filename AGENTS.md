# Dotfiles

These are my dotfiles

The way they are structured we use gnu `stow` against any of the root directories.

For example I did `stow zsh` and there is a `zsh/.config/zsh/*` files because they were stowed
