onboarding = false
# Herdr configuration
# Stowed with: stow herdr  ->  ~/.config/herdr/config.toml
#
# See `herdr --default-config` for the full annotated reference.
# Apply edits to the running server with `hc` (herdr server reload-config).
# The server parses this file only at startup, so nothing here is live until
# that runs.
#
# Binding syntax:
#   "prefix+n"      -> requires the prefix key first (default ctrl+b)
#   "ctrl+alt+n"    -> direct chord, works straight from terminal mode
# Anything left commented out keeps its documented default, so every stock
# prefix binding (prefix+c, prefix+w, prefix+hjkl ...) still works alongside
# the direct chords below.

[theme]
# Match Zed's system-aware theme pair. Herdr re-queries the host terminal when
# focus returns, so macOS appearance changes switch both layers together.
# `name` is the fallback if the host does not report its appearance.
name = "one-dark"
auto_switch = true
dark_name = "one-dark"
light_name = "rose-pine-dawn"

[keys]
# --- Tabs ---------------------------------------------------------------
# ctrl+alt+n opens a tab, ctrl+alt+x closes it, ctrl+alt+h/l walk between
# them (same left/right feel as vim's h/l).
new_tab = "ctrl+alt+n"
close_tab = "ctrl+alt+x"
previous_tab = "ctrl+alt+h"
next_tab = "ctrl+alt+l"
rename_tab = "ctrl+alt+r"

# --- Workspaces ("spaces") ---------------------------------------------
# ctrl+alt+j/k move down/up the workspace list in the sidebar, mirroring
# the tab motion one axis over. ctrl+alt+shift+w opens the workspace picker
# to jump straight to one by name (plain ctrl+alt+w closes a pane, browser
# style).
next_workspace = "ctrl+alt+j"
previous_workspace = "ctrl+alt+k"
workspace_picker = "ctrl+alt+shift+w"
rename_workspace = "ctrl+alt+shift+r"
new_workspace = "ctrl+alt+shift+n"

# --- Agents -------------------------------------------------------------
# Shift on the same j/k axis hops between agent rows instead of workspaces.
next_agent = "ctrl+alt+shift+j"
previous_agent = "ctrl+alt+shift+k"

# --- Panes --------------------------------------------------------------
# hjkl is already spent on tabs/workspaces, so pane focus uses the arrows.
focus_pane_left = "ctrl+alt+left"
focus_pane_down = "ctrl+alt+down"
focus_pane_up = "ctrl+alt+up"
focus_pane_right = "ctrl+alt+right"
split_vertical = "ctrl+alt+v"
split_horizontal = "ctrl+alt+minus"
# ctrl+alt+w closes the focused pane; the tab only goes away once its last
# pane is gone. ctrl+alt+x is the deliberate "close the whole tab" chord.
close_pane = "ctrl+alt+w"
# rename_pane has no direct chord on purpose. It sat on ctrl+alt+shift+p, but
# that swallowed cmd+alt+shift+p before the "jump to the p* space" command below
# could see it -- the one letter of the 26 that misfired that way (cmd+alt+shift+r
# reaches its command fine despite rename_workspace next door). Renaming a pane
# is rare enough to live on the stock prefix binding, ctrl+b then shift+p.
zoom = "ctrl+alt+z"

# --- Chrome -------------------------------------------------------------
toggle_sidebar = "ctrl+alt+b"

# --- Indexed tabs -------------------------------------------------------
# ctrl+alt+1..9 jumps straight to the Nth tab -- browser muscle memory, and the
# same ctrl+alt chord family as everything above. Bare alt+1..9 stays off the
# table: Raycast claims it globally. ctrl+alt+digit has no legacy terminal
# encoding, but herdr pushes the kitty keyboard enhancement flags and Ghostty
# answers in CSI-u (ctrl+alt+1 -> CSI 49;7u), so the chord arrives intact.
switch_tab = "ctrl+alt+1..9"

# --- Indexed spaces -----------------------------------------------------
# cmd+alt+shift+1..9 jumps straight to the Nth space in the sidebar: the indexed
# sibling of ctrl+alt+j/k (one row at a time) and ctrl+alt+u/i (the two ends).
# Tabs already own the ctrl+alt+digit chord, so the space axis moves to the cmd
# side of the keyboard rather than stacking a fourth modifier on top of it.
#
# Ghostty keeps bare cmd+1..9 for its own goto_tab, but leaves cmd+alt+shift+digit
# unbound, and its kitty-protocol encoding carries the super bit
# (cmd+alt+shift+1 -> CSI 49;12u), so the chord arrives intact -- the same
# mechanism switch_tab above relies on.
switch_workspace = "cmd+alt+shift+1..9"

# ctrl+alt+u/i -- jump to the first/last space in the sidebar. ctrl+alt+j/k walk
# the list one row at a time; these are the two ends of that same axis, and u/i
# sit directly above j/k on the home row, so the "one row" and "all the way"
# motions stay in one finger cluster. (This used to be a lone ctrl+alt+g for the
# bottom end, on vim's G; keeping both ends means the pair reads better as u=up,
# i=down than as g/shift+g.)
#
# Herdr has no first_workspace/last_workspace action to bind -- switch_workspace
# is an indexed 1..9 binding, which cannot name "whichever is last" -- so these go
# through a script that reads `workspace list` and focuses the edge of the array.
# `type = "shell"` gets spawned detached with the server's environment, so the
# command is wrapped in an explicit /bin/sh rather than trusting $PATH to contain
# ~/.local/bin.
#
# ctrl+i is legacy-encoded as Tab, but herdr pushes the kitty keyboard flags and
# Ghostty answers in CSI-u (ctrl+alt+i -> CSI 105;7u), so the chord is distinct
# from a real Tab -- same mechanism the ctrl+alt+1..9 binding relies on.
#
# ctrl+alt+g used to sit on the last-space job (and lazygit-in-a-popup before
# that); it now has no binding at all.
[[keys.command]]
key = "ctrl+alt+u"
type = "shell"
description = "First space"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-edge first"'

[[keys.command]]
key = "ctrl+alt+i"
type = "shell"
description = "Last space"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-edge last"'

# --- Spaces by initial ---------------------------------------------------
# cmd+alt+shift+a..z -- jump to the topmost space whose name starts with that
# letter. This is the letter half of the indexed binding above: cmd+alt+shift+digit
# addresses a space by its position in the sidebar, cmd+alt+shift+letter by its
# initial, so the whole space axis stays on one chord family and the cmd side of
# the keyboard keeps meaning "spaces" the way ctrl+alt means "tabs and panes".
# hjkl is fair game here -- ctrl+alt+hjkl owns the tab/workspace motion, and this
# is a different modifier set, so nothing collides.
#
# Herdr has no built-in "focus the space whose name starts with X" action
# (workspace_picker is an interactive fuzzy prompt, switch_workspace is indexed
# 1..9), so each letter runs the same script with its letter as the argument.
# First match in sidebar order wins; a letter that matches nothing is a silent
# no-op. `type = "shell"` is spawned detached with no tty, so the script reports
# to ~/.local/state/herdr-space-letter.log rather than stderr.
#
# Yes, 26 near-identical blocks: the binding syntax has a 1..9 range for indexed
# actions but no a..z range for commands, and no way to hand the pressed key to
# the command, so each one has to be spelled out.

[[keys.command]]
key = "cmd+alt+shift+a"
type = "shell"
description = "Space a*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter a"'

[[keys.command]]
key = "cmd+alt+shift+b"
type = "shell"
description = "Space b*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter b"'

[[keys.command]]
key = "cmd+alt+shift+c"
type = "shell"
description = "Space c*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter c"'

[[keys.command]]
key = "cmd+alt+shift+d"
type = "shell"
description = "Space d*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter d"'

[[keys.command]]
key = "cmd+alt+shift+e"
type = "shell"
description = "Space e*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter e"'

[[keys.command]]
key = "cmd+alt+shift+f"
type = "shell"
description = "Space f*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter f"'

[[keys.command]]
key = "cmd+alt+shift+g"
type = "shell"
description = "Space g*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter g"'

[[keys.command]]
key = "cmd+alt+shift+h"
type = "shell"
description = "Space h*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter h"'

[[keys.command]]
key = "cmd+alt+shift+i"
type = "shell"
description = "Space i*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter i"'

[[keys.command]]
key = "cmd+alt+shift+j"
type = "shell"
description = "Space j*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter j"'

[[keys.command]]
key = "cmd+alt+shift+k"
type = "shell"
description = "Space k*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter k"'

[[keys.command]]
key = "cmd+alt+shift+l"
type = "shell"
description = "Space l*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter l"'

[[keys.command]]
key = "cmd+alt+shift+m"
type = "shell"
description = "Space m*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter m"'

[[keys.command]]
key = "cmd+alt+shift+n"
type = "shell"
description = "Space n*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter n"'

[[keys.command]]
key = "cmd+alt+shift+o"
type = "shell"
description = "Space o*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter o"'

[[keys.command]]
key = "cmd+alt+shift+p"
type = "shell"
description = "Space p*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter p"'

[[keys.command]]
key = "cmd+alt+shift+q"
type = "shell"
description = "Space q*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter q"'

[[keys.command]]
key = "cmd+alt+shift+r"
type = "shell"
description = "Space r*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter r"'

[[keys.command]]
key = "cmd+alt+shift+s"
type = "shell"
description = "Space s*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter s"'

[[keys.command]]
key = "cmd+alt+shift+t"
type = "shell"
description = "Space t*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter t"'

[[keys.command]]
key = "cmd+alt+shift+u"
type = "shell"
description = "Space u*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter u"'

[[keys.command]]
key = "cmd+alt+shift+v"
type = "shell"
description = "Space v*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter v"'

[[keys.command]]
key = "cmd+alt+shift+w"
type = "shell"
description = "Space w*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter w"'

[[keys.command]]
key = "cmd+alt+shift+x"
type = "shell"
description = "Space x*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter x"'

[[keys.command]]
key = "cmd+alt+shift+y"
type = "shell"
description = "Space y*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter y"'

[[keys.command]]
key = "cmd+alt+shift+z"
type = "shell"
description = "Space z*"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-letter z"'

# ctrl+alt+; -- new tab already running the agent I pressed from, same cwd, same
# flags. ctrl+alt+n's shell-tab sibling: ';' sits right of 'l', so it lands in
# the same right-hand cluster as the h/l tab motion.
#
# There is no built-in action for this, so it goes through a script that reads
# the focused pane and then drives `tab create` + `agent start`. `type = "shell"`
# gets spawned detached with the server's environment, so the command is wrapped
# in an explicit /bin/sh rather than trusting $PATH to contain ~/.local/bin.
[[keys.command]]
key = "ctrl+alt+semicolon"
type = "shell"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-agent-tab"'

[[keys.command]]
key = "ctrl+alt+shift+f"
type = "popup"
description = "Space subtitle"
command = '/bin/sh -c "exec $HOME/.local/bin/herdr-space-folder"'
width = "60%"
height = "20%"

[ui]
# Borders and navigate-mode UI default to cyan, which would be a third hue
# alongside the tab blue and the grey chrome. Pin it to the same steel blue.
accent = "#81a2be"

# ctrl+alt+n should feel like a browser new-tab, so skip the name prompt.
# Flip to true to be asked for a tab name on every create.
prompt_new_tab_name = false

# Workspaces are long-lived and picked by name (ctrl+alt+shift+w), so always
# ask for one on ctrl+alt+shift+n instead of taking a generated name.
prompt_new_workspace_name = true

[ui.sidebar.spaces]
rows = [["state_icon", "workspace"], ["$folder", "git_status"]]
